# Release workspace

Release manifests, packaging definitions, signing metadata schemas, and
reproducibility instructions live here. Local packaged artifacts are produced
under `output/` and are intentionally ignored.
