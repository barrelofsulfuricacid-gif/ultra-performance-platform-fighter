# Optimization analyses

Every performance change begins as a measured analysis in `pending/`. Move it
to `merged/` only when comparable performance and correctness evidence support
adoption. Move failed, neutral, or superseded work to `discarded/` and retain
the evidence.

Start analyses from `docs/templates/optimization_analysis.md`.
