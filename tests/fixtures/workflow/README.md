# Workflow validation fixtures

These records are synthetic, valid examples used only by
`tools/verify_m1_workflow.sh`. Their repeated hexadecimal hashes are fixtures,
not claims about repository commits or shipped builds.

The paired verifier and human-feedback files model the required
unfixed-to-fixed bookkeeping lifecycle. Production records live in their
top-level workflow directories.
