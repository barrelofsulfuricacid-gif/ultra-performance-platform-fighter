# Performance reports

Accepted milestone and optimization performance reports live here. Reports
must include the benchmark protocol, environment fingerprint, distribution
summary, compatible baseline, correctness evidence, and raw evidence location.

Automatic per-commit reports remain under `performance/local/`.
