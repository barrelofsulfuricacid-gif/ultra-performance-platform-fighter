# Performance graphs

Reproducible milestone graph exports live here. Each graph must identify the
source data, compatible baseline key, generating command/version, and commit.

Exploratory or per-commit graphs remain under `performance/local/`.
