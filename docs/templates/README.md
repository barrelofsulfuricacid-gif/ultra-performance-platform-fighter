# Workflow templates

These templates define stable metadata and evidence headings for M1 workflow
records. Copy the closest template, replace every angle-bracket placeholder,
and preserve headings that do not apply by writing `Not applicable` with a
reason.

`tools/verify_m1_workflow.sh` checks that the canonical template set and its
valid lifecycle fixtures remain usable.
