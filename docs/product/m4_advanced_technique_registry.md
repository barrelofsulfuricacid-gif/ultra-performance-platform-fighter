# M4 SSBM advanced-technique registry

- **Registry schema:** 1
- **Pinned source:** [SmashWiki advanced-technique table, revision 2048934](https://www.ssbwiki.com/index.php?title=Advanced_technique&oldid=2048934#List_of_advanced_techniques)
- **Captured:** 2026-07-28
- **Scope:** 61 unique non-character-specific rows marked available for SSBM
- **M4 acceptance:** Blocked until every row is `verified`

The source table lists dash-dancing twice. This registry records it once.
Character-specific techniques are excluded here and are a separate M8 gate.
Every target is M4.4; no row may be deferred to a later milestone.

Statuses are monotonic evidence states:

- `planned`: one or more production mechanics or fixtures are absent.
- `primitive-ready`: relevant canonical mechanics exist, but the complete
  technique is not yet playable.
- `playable`: the technique can be performed through ordinary match/browser
  input, but some required positive/negative, cross-target, replay, or owner
  evidence is incomplete.
- `verified`: the complete behavior has deterministic positive and negative
  oracles, a verifier-readable trace, save/load/replay/rollback and
  native/WebAssembly evidence, and a passing browser recipe.

Planned recipes describe the eventual human gate; they are not claims that the
current build can perform the technique.

| # | Technique | Status | Target | Dependencies and supporting configuration | Automated evidence | Browser playtest recipe |
|---:|---|---|---|---|---|---|
| 1 | Approach | playable | M4.4 | The ordinary reduced-stick walk, default-stage neutral start, standing attacks, whiff recovery, and a jab-first responder compose into a legal simple approach; broader dash, aerial, grab, projectile, and platform options remain future coverage | `tests/sim/test_m4_combat.c` walks from the default 16-unit separation into the safe attack band, converts the responder's jab whiff with strong attack, and proves an overextended close approach is intercepted; `src/web_client/m4_playtest.c` repeats both ordinary-input routes before browser readiness | From Reset, use reduced horizontal input to walk toward the opponent. Stop just outside their light-attack reach, have them jab, and use strong attack to convert the whiff; repeat while walking too close and confirm the responder's jab stops the approach first |
| 2 | Auto-canceling | playable | M4.4 | The original aerial has an explicit landing-lag-active window; landing outside it takes generic four-tick landing instead of 12-tick aerial landing lag | `tests/sim/test_m4_movement.c` covers early auto-cancel and the normal-lag comparison; the browser aerial startup probe covers a default-content no-lag full-hop route and the lagged short-hop route | Full hop and use the aerial late enough to land during its startup auto-cancel frames; confirm generic `LANDING`, then short-hop aerial and fast-fall into the active landing-lag window to confirm `AERIAL LANDING` instead |
| 3 | Bat dropping | playable | M4.4 | The fixed-capacity original Relay Rod has data-defined pickup, carry, grounded/aerial drop, an airborne item hitbox, authored damage/knockback, pickup lockout, lifetime, despawn, and reset; light plus shield owns pickup/drop priority only while the item route is legal | `tests/sim/test_m4_item.c` proves an aerial drop onto a legal target, the spacing miss, typed pickup/drop/hit/reset events, damage and bounce, save/load future hashes/events, replay verification, structured/compact RL visibility, and despawn/reset; `src/web_client/m4_playtest.c` repeats the hit and spacing negative before readiness, while `tests/web/test_m4_playtest.c` exercises the live lab pickup and throw | Walk left from the default start to the gold Relay Rod, press light plus shield to pick it up, jump and drift over the opponent, then press light plus shield again to drop it. Confirm the rod's active box hits and bounces; repeat without drifting over the opponent and confirm it lands without damage |
| 4 | Boost grab | playable | M4.4 | A production dash attack can be canceled into the production grab on action frames 2–4, transferring its authored momentum; ordinary run-to-grab remains slower and direct initial-dash grab remains rejected | `tests/sim/test_m4_combat.c` proves the ordinary range whiff, exact three-frame cancel window, boosted capture at the extended range, late-cancel rejection, production dash-attack hit, typed hit identity, invalid data, and mid-route save/load with equal future hashes/events; `src/web_client/m4_playtest.c` repeats the ordinary, boosted, late, and dash-attack-hit routes before readiness | Reach `RUN`, press light to start `DASH ATTACK`, then add shield while light remains held on action frame 2, 3, or 4. Confirm the cancel enters `GRAB`, preserves the faster forward slide, and captures at a range where light-plus-shield directly from `RUN` whiffs. Wait one extra frame and confirm the dash attack continues instead |
| 5 | Camping | playable | M4.4 | The existing one-slot Pulse Bolt, grounded fire recovery, fixed stage positions, and deterministic match clock compose the researched [projectile-camping](https://www.ssbwiki.com/Camping) tactic against an ordinary approach-and-jab policy; the same policy without projectiles is the anti-camp control | `tests/sim/test_m4_projectile.c` runs the production path for 180 ticks: seven legal fire actions produce six hits while the camper takes zero damage and never comes within 10.58 units, then the no-fire control lets the same responder reach and land three physical hits; `src/web_client/m4_playtest.c` independently repeats both bounded traces before browser readiness; `tools/verify_m4_projectile.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | Move to one side with the opponent across the stage. Fire one Pulse Bolt with fresh Special whenever the prior bolt has resolved and keep the opponent outside melee range while they approach and jab. Reset and omit every projectile; confirm the same opponent now closes the distance and hits |
| 6 | Chain grab | playable | M4.4 | Four data-defined directional throws now use the production grab, hitlag/hitstun, DI/SDI, pursuit, and ordinary regrab paths; chain grab remains an emergent route rather than a technique-only action | `tests/sim/test_m4_combat.c` covers exact startup/release/recovery for all four throws, a low-percent three-down-throw route with two legal regrabs, an earliest-standing-regrab whiff at 96% with outward DI, and mid-chain save/load with equal future hashes and events; `src/web_client/m4_playtest.c` repeats every directional throw, the complete chain, and the neutral-input negative before readiness, while `tests/web/test_m4_playtest.c` checks the live typed throw event and cleared links | Press light plus shield while grounded to grab. During `GRAB HOLD`, hold full down and freshly press either attack; pursue the launch and repeat until two regrabs complete. Then build the target to high percent, hold outward DI as defender, and confirm the earliest standing regrab whiffs after the down throw |
| 7 | Charge storage canceling | playable | M4.4 | The original data-defined Arc Reservoir starts or resumes from full-up plus fresh special, accumulates a canonical 120-tick charge, stores through shield, and exposes the researched [charge storage cancel](https://www.ssbwiki.com/Charge-canceling): release shield during the four-tick store animation to regain the full grounded moveset while retaining charge; holding shield commits to ordinary shield, Attack releases charge-scaled damage, and a hit during charge/store clears it | `tests/sim/test_m4_charge.c` covers default/invalid data, accumulation and clamp, early-release attack, held-shield negative, exact resume, low/full release scaling, interruption loss, 694-byte mid-store save/load future equality, replay verification, and structured/compact RL visibility; `src/web_client/m4_playtest.c` repeats charge, store cancel, resumed charge, and release before readiness; `tools/verify_m4_charge.sh` compiles the strict native oracle | Hold full up and freshly press special (`E`, `;`, or top face) to enter `ARC RESERVOIR CHARGE`. Press shield to enter `ARC RESERVOIR STORE`, release shield before four ticks finish, and confirm the stored meter remains while an immediate ordinary attack works. Re-enter with up plus fresh special, then press Attack to release the scaled hit; hold shield through the store animation for the negative, and let an opponent hit the charging fighter to confirm the stored value is lost |
| 8 | Cross-up | playable | M4.4 | Ordinary short-hop neutral-aerial route, airborne pass-through positioning, facing lock, physical shield block, and front-side control; production grab now exists, but the focused shield-grab comparison remains future coverage | `tests/sim/test_m4_combat.c` covers behind-shield completion, early whiff, front-side block control, and mid-aerial save/load with 48 future hashes; `src/web_client/m4_playtest.c` repeats all three routes from ordinary movement on the default stage | Face away in front of a shielding target, short hop through them, release the directional stick, then use the neutral light aerial on descent; confirm the block leaves the attacker behind while still facing away. Attack immediately for the whiff case, then repeat facing toward the target and confirm the block leaves the attacker in front |
| 9 | Dash cancel | playable | M4.4 | Production jump cancel from initial dash plus run-cancel routes into a momentum-preserving crouch or dense shield; crouch exposes immediate ground actions, while initial-dash shield and run-turnaround crouch remain excluded | `tests/sim/test_m4_movement.c` covers jump, crouch, shield, immediate attack, exact facing/momentum, initial-dash shield and run-turnaround negative cases, and mid-crouch save/load hash continuation; `src/web_client/m4_playtest.c` repeats the ordinary-input routes before browser readiness | During initial dash, jump and confirm `JUMP SQUAT`. Reach `RUN`, press down to slide into `CROUCH`, then immediately attack; repeat from `RUN` with shield. Try shield during `INITIAL DASH` and down during `RUN TURNAROUND` for the rejected cases |
| 10 | Dash-dancing | verified | M4.4 | Production initial-dash reversal, normalized full-axis keyboard input, replay/state schema | `tests/sim/test_m4_movement.c`; `src/web_client/m4_playtest.c`; `tools/verify_m4_movement.sh`; `tools/verify_m4_browser.sh` | Tap full left/right during the ten-tick initial dash and confirm every reversal remains `INITIAL DASH`; repeat after `RUN` and confirm it becomes `RUN TURNAROUND` instead |
| 11 | Dashing shield | playable | M4.4 | Production run-to-shield preserves run momentum under ordinary traction; the data-defined eight-tick minimum hold and 15-tick release distinguish a one-tick tap from a held shield stop without adding presentation-only state | `tests/sim/test_m4_combat.c` compares exact tap/held position and velocity, idle no-travel, shield health, the full release duration, and mid-route save/load future hashes; `src/web_client/m4_playtest.c` repeats the tap, held, and idle routes before browser readiness | Reach `RUN`, tap shield for one tick, and confirm the fighter slides through the minimum hold into `SHIELD RELEASE`, then becomes actionable after 15 release ticks. Repeat while holding shield and confirm the same traction path remains `SHIELD`; tap shield from idle for the no-travel negative case |
| 12 | Double jump cancel | playable | M4.4 | The default fighter's fresh-edge air jump enters a data-defined six-tick `DELAYED AIR JUMP`; a fresh light or strong aerial in that action cancels the remaining upward velocity before gravity, while simultaneous or first-late input retains the ordinary arc without technique-only per-player state | `tests/sim/test_m4_movement.c` proves the default/disabled content contract, exact first and last cancel frames, light and strong aerials, simultaneous and first-late negatives, shorter landing arc, content hashing, and a mid-window save/load with equal future hashes through landing; `src/web_client/m4_playtest.c` repeats the light cancel, late arc, landing comparison, and simultaneous negative before browser readiness | Tap jump and release during jump squat, freshly press jump once airborne, then press light or strong attack while `DELAYED AIR JUMP` shows action frames 0–5; confirm upward speed is canceled and the fighter descends immediately. Wait until `AIRBORNE` returns before attacking to keep the full arc, or press jump and attack simultaneously and confirm the air jump is not consumed |
| 13 | Double jump cancel counter | playable | M4.4 | The authored delayed air jump now has knockback-based armor through a 20-hitstun threshold: a qualifying physical hit still applies damage and ordinary hitlag, preserves the existing double-jump trajectory and cancel timer, then permits an immediate aerial counter; the same hit outside the six-tick window and a stronger 34-hitstun aerial launch normally | `tests/sim/test_m4_combat.c` proves the exact 16-hitstun boundary, disabled and invalid data, weak armored contact, damage/hitlag with zero launch, frozen trajectory, delayed-action resume, a real aerial counter-hit, late and strong-hit negatives, content hashing, and a 694-byte mid-hitlag save/load with equal future hashes/events; `src/web_client/m4_playtest.c` repeats the armored counter, late-window failure, and strong-hit armor break before restoring default content | Have both fighters jump close together. Start player 1's light aerial, then freshly double-jump with player 2 as its active frame arrives; confirm player 2 takes damage and freezes but keeps the double-jump arc, then press light during the resumed `DELAYED AIR JUMP` to cancel upward momentum and counter-hit. Repeat after `AIRBORNE` returns, then with player 1's strong aerial, and confirm both launch player 2 normally |
| 14 | Drop cancel | playable | M4.4 | The production nine-tick pass-through timer, same-tick fast-fall suppression, first-airborne-frame light aerial, attacker hitlag countdown, and validated five-eighths-unit snap distance compose into a same-platform landing without a technique-only action | `tests/sim/test_m4_combat.c` proves the frame-perfect hit, exact same-platform snap and 12-tick aerial landing lag, one-tick-late hit and whiff fall-through negatives, invalid-data rejection, and mid-route save/load future hashes; `tests/sim/test_m4_movement.c` fixes the drop/fast-fall boundary; `src/web_client/m4_playtest.c` repeats the positive and one-tick-late routes through ordinary input before browser readiness | Put both fighters close together on the pass-through platform. Keep the target still; with the attacker, press down to drop and press light attack on the first airborne tick. The hitlag-assisted route returns the attacker to `AERIAL LANDING` on the same platform. Wait one extra tick before attacking, or move the target out of range, and confirm the attacker falls through instead |
| 15 | Edge dashing | playable | M4.4 | Production 37-tick ledge invulnerability, ledge jump, inward air dodge, and momentum-preserving special landing compose through ordinary input | `tests/sim/test_m4_movement.c` covers the complete ledge-jump-to-air-dodge route, exact special-landing lock, actionable/invulnerable overlap, attack follow-up, expired-invulnerability negative case, and mid-route save/load continuation; `src/web_client/m4_playtest.c` and `tools/verify_m4_browser.sh` repeat the positive and expired-window routes before browser readiness | From `LEDGE HANG`, jump, hold inward while rising above stage height, then press down-inward plus a fresh trigger. Confirm `SPECIAL LANDING`, inward slide, ten locked ticks, and an actionable frame while the dashed invulnerability ring remains; wait on ledge for the ring to expire and repeat for the negative case |
| 16 | Edge hopping | playable | M4.4 | Production 37-tick ledge invulnerability, ledge release, fresh-edge air jump, and aerial action compose through ordinary input while facing remains fixed | `tests/sim/test_m4_movement.c` covers down-release into an immediate inward air jump, air-jump consumption, exhausted-jump rejection, aerial follow-up, retained invulnerability, neutral-hang comparison, and mid-route save/load continuation; `src/web_client/m4_playtest.c` and `tools/verify_m4_browser.sh` run the same positive and negative startup routes | Grab a ledge while facing inward, wait through `LEDGE CATCH`, press down and release it, then on the next tick press jump while holding inward. Confirm `air jumps` changes from 1 to 0, follow with an aerial, and compare neutral hang and an exhausted second jump |
| 17 | Fox-trotting | playable | M4.4 | The placeholder fighter's data-defined initial-dash speed/window, fresh same-direction input edge, neutral release, traction, and run transition compose through ordinary input | `tests/sim/test_m4_movement.c` covers four consecutive same-direction dash/release bursts, exact action-timer and velocity restart, forward travel, held-input run and reduced-magnitude walk negative cases, and mid-rhythm save/load hash continuation; `src/web_client/m4_playtest.c` repeats the positive and both negative routes before browser readiness | Rhythmically tap and release one full horizontal direction and confirm each tap returns to tick 1 of `INITIAL DASH` without reaching `RUN`; then hold the direction through the ten-tick window to reach `RUN`, and repeat with Shift/reduced magnitude to confirm `WALK` instead |
| 18 | Glide toss | playable | M4.4 | Carrying the Relay Rod into the data-defined roll action frames 0–4 allows a fresh attack to throw it without replacing roll velocity; the item's four directional velocities inherit three-quarters of fighter momentum, while action frame 5 and later retain the roll and held item | `tests/sim/test_m4_item.c` proves the last legal frame, the first-late rejection, retained positive fighter velocity, added item velocity, typed throw identity, and deterministic state/replay/RL contracts; `src/web_client/m4_playtest.c` repeats both timing outcomes before readiness | Pick up the Relay Rod, press shield plus a full horizontal direction to roll, then freshly press light during roll action frames 0–4. Confirm the rod gains the roll's momentum while the fighter keeps sliding. Wait until frame 5 and confirm the attack is ignored, the roll continues, and the item remains held |
| 19 | Gimp | playable | M4.4 | The researched [gimp](https://www.ssbwiki.com/Gimp) emerges from the original once-per-airtime Vector Ascent, ordinary air drift/jumps/dodge/ledge grab, interruptible aerial and Prism Burst hits, blast zones, stocks, and typed KOs. The recovery succeeds when unchallenged but cannot be reused after an edgeguard spends or interrupts it; no gimp-only action or outcome is hardcoded | `tests/sim/test_m4_movement.c` independently proves Vector Ascent entry, steering, consumption, blocked reuse, landing restoration, save/load future equality, and RL visibility; `tests/sim/test_m4_reflector.c` proves the ordinary-input offstage downward-hit/KO and unchallenged-recovery control; `tests/sim/test_m4_combat.c` and `tests/sim/test_m4_match.c` cover aerial interruption and stock/KO primitives; browser readiness and `tests/web/test_m4_playtest.c` exercise the live recovery and Prism Burst routes. Per the emergent-technique policy, these constituent oracles are reused instead of adding a gimp-specific harness | Knock the opponent offstage and have them hold inward/up plus freshly press Special to enter `VECTOR ASCENT`. Follow and intercept the rise with a light aerial or `PRISM BURST AIR`; confirm the spent recovery cannot be used again and the opponent misses the stage or loses a stock. Reset and leave the identical recovery unchallenged; confirm it reaches the ledge or stage and becomes ready again |
| 20 | Infinite | playable | M4.4 | The fast-jab fixture's ordinary earliest-idle attack loop, near-zero knockback growth, 999% damage cap, and solid-wall collision compose a theoretically unbounded [infinite](https://www.ssbwiki.com/Infinite) without an infinite-only action; open-stage outward DI remains the explicit escape policy | Existing `tests/sim/test_m4_combat.c` proves a 21-jab uninterrupted prefix with no actionable defender frame, mid-chain save/load future equality, and an outward-DI active whiff; wall collision and capped damage are independently canonical, and `src/web_client/m4_playtest.c` repeats the bounded prefix and DI break before readiness | In the fast-jab fixture, move the target against the solid wall, face the wall, and press jab again on each earliest idle frame instead of using the strong finisher; after damage reaches its cap, continue the same input loop and confirm the pinned target never becomes actionable. Repeat in open space with outward DI to break the loop |
| 21 | Instant double jump | playable | M4.4 | Production jump squat, binary first jump, fresh-edge air jump, and remaining-air-jump state compose through ordinary input on the first legal airborne frame | `tests/sim/test_m4_movement.c` covers the exact first-airborne-frame velocity/position, launch-tick exclusion, held-input non-repeat, exhausted-jump rejection, and mid-IDJ save/load continuation; `src/web_client/m4_playtest.c` and `tools/verify_m4_browser.sh` run the positive and held-input routes before browser readiness | Tap and release one jump key during the three-tick jump squat, then press the other jump key on the first airborne frame. Confirm the fighter immediately takes the double-jump arc while the live `air jumps` counter changes from 1 to 0; hold one jump key through takeoff for the negative case and confirm the counter remains 1 |
| 22 | Jab cancel | playable | M4.4 | The production two-hit neutral sequence exposes a data-defined action-frame 4–7 decision window: a fresh shield cancels the first jab on hit or whiff, while fresh light selects the independently authored final hit; early-held and first-late inputs remain rejected without technique-only mutable state | `tests/sim/test_m4_combat.c` proves exact begin/end cancels, hit/whiff parity, early-held and first-late rejection, final-hit damage and typed identity, invalid data, and mid-window save/load with equal future hashes/events; `src/web_client/m4_playtest.c` repeats both cancels, both timing negatives, and the final-hit route before readiness | Press light from standing to enter `GROUND ATTACK`, then freshly press shield on action frame 4 through 7 and confirm immediate `SHIELD`; repeat once close enough to hit and once at whiff range. Hold shield from frame 3 into frame 4, then freshly press it on frame 8, and confirm both jabs continue. Instead freshly press light during frames 4–7 and confirm `JAB FINAL` deals the independently typed second hit |
| 23 | Jab reset | playable | M4.4 | A vulnerable `DOWN_WAIT` target hit by an authored weak attack at or below 7% damage and 12 hitstun ticks enters a 12-tick low `RESET_BOUND`, then a 30-tick vulnerable `FORCED_GETUP`; ordinary getup invulnerability and hitlag SDI/ASDI provide the documented avoidance routes without technique-only mutable state | `tests/sim/test_m4_combat.c` proves typed 6% and exact 7%/12-hitstun resets, exact bound/getup timing and control lock, over-damage and tumble rejection, same-tick invulnerable getup avoidance, SDI escape into an aerial, invalid data, and mid-bound save/load with 64 equal future hashes/events through a real forced-getup punish; `src/web_client/m4_playtest.c` repeats the positive, getup, over-damage, and SDI routes before readiness | Strong-launch the target, omit tech, wait for `DOWN WAIT`, then jab before a getup choice and confirm `RESET BOUND` followed by 30 vulnerable `FORCED GETUP` ticks. Repeat with the target choosing a getup on the collision tick to avoid the jab, then use two hitlag SDI pulses plus ASDI away and confirm the target remains airborne when the bound expires and may aerial instead of being forced up |
| 24 | Juggling | playable | M4.4 | Production strong launch, tumble and wall bounce, ordinary pursuit/full hop, light aerial, DI, and directional air dodge compose into a launcher-to-airborne-follow-up route; longer percent-dependent chains and stock conversions remain | `tests/sim/test_m4_combat.c` proves the 12% launcher into an 8% aerial before any landing, mid-launch save/load with equal future hashes, and a DI-plus-air-dodge escape that makes the active follow-up whiff; `src/web_client/m4_playtest.c` repeats both routes through ordinary input on the default moving-platform stage before browser readiness | Place the target near center with the attacker just inside strong range. Strong-launch, follow the airborne path, then full hop and light-aerial the descending target before it touches a surface. Repeat while the target DIs away and uses a fresh directional air dodge; confirm the same aerial whiffs and the target keeps only the launch damage |
| 25 | Jump cancel throw | playable | M4.4 | A held Relay Rod can be thrown from production jump squat before takeoff: the throw replaces the pending jump, keeps the fighter grounded, preserves initial-dash momentum, and transfers it to the item; once airborne, the same input remains an ordinary aerial item throw | `tests/sim/test_m4_item.c` proves dash-to-jump-squat throw, grounded cancellation, fighter/item momentum, typed throw identity, and the first-airborne-frame negative; `src/web_client/m4_playtest.c` repeats both ordinary-input routes before readiness | Pick up the Relay Rod, dash, press jump, then freshly press light while `JUMP SQUAT` is visible. Confirm the fighter stays grounded in `ITEM THROW` while both fighter and item retain forward momentum. Wait until the fighter becomes `AIRBORNE` before pressing light and confirm the item still throws but the jump is not canceled |
| 26 | Jump-canceled grab | playable | M4.4 | A fresh light attack while holding shield selects the production standing grab from jump squat; initial dash can enter jump squat first, the grab retains positive dash momentum under traction, shield does not block capture, and direct dash or airborne grab input remains rejected | `tests/sim/test_m4_combat.c` covers ordinary and shielded capture, exact two-frame grabbox, percent scaling/cap, natural and fresh-input mash escape, same-team rejection, lower-port priority, direct-dash and late-airborne negatives, retained momentum, mid-hold save/load, equal future hashes, and typed grab/escape events; `src/web_client/m4_playtest.c` repeats the positive and negative routes before readiness, while `tests/web/test_m4_playtest.c` verifies live rendered links and the typed grab event | Begin an initial dash, press jump, then press light attack while holding shield during `JUMP SQUAT`; confirm `GRAB`, retained forward slide, `GRAB HOLD`/`GRABBED`, and the cyan grabbox. Try the same light-plus-shield input directly during dash or after takeoff and confirm no grab; as the victim, alternate fresh inputs to shorten the `MASH OUT` timer |
| 27 | Jump-cancelling | playable | M4.4 | During the production three-tick jump squat, full up plus a fresh light or strong attack selects the standing strong attack without takeoff and preserves inherited dash momentum; the same router already supports standing grab and Relay Rod item throw, while neutral/below-threshold attacks and first-airborne-frame input retain their ordinary jump/aerial routes | `tests/sim/test_m4_combat.c` proves both attack buttons, exact full-up threshold rejection, neutral-input rejection, retained dash momentum, first-airborne-frame aerial behavior, a real strong hit, and mid-action save/load future hash/event equality; the focused grab and item oracles cover the other legal jump-cancel targets, and `src/web_client/m4_playtest.c` repeats both positive attack buttons plus all three negative routes before readiness | Start a dash, press jump, then during `JUMP SQUAT` hold full up and freshly press light or strong. Confirm `STRONG ATTACK` begins on the ground while the dash slide remains. Repeat with neutral or slightly sub-threshold up and confirm jump squat continues; wait until the first airborne frame and confirm the ordinary aerial instead. Light plus shield and a held-item attack remain the grab and item-throw variants |
| 28 | Kill confirm | playable | M4.4 | A data-defined fast, low-launch jab and the production strong finisher compose through ordinary input at a high-percent top-blast threshold; percent, hitlag/hitstun lock, DI, stock loss, typed KO attribution, and wide-stage recovery use the canonical paths | `tests/sim/test_m4_combat.c` builds 120% through legal jabs, proves the 126% setup stays locked into a 138% KO, compares every future hash from setup hit through stock loss, and covers both an 18% non-KO control and a 126% DI escape with an active whiff; `src/web_client/m4_playtest.c` runs the same three routes in the WebAssembly-facing sim and restores default content before browser readiness | In the kill-confirm fixture, build the target to 120%, then jab once and buffer the strong finisher as soon as recovery ends; confirm the defender never becomes actionable and loses a stock at 138%. Repeat from 0% and confirm the 18% route lands safely, then repeat at 120% while holding outward DI and confirm the active finisher whiffs at 126% |
| 29 | L-cancelling | playable | M4.4 | The light and strong aerial routes, independent fresh-trigger age, exact seven-frame eligibility window, and integer-divided normal/reduced landing lag are production mechanics | `tests/sim/test_m4_movement.c` covers ages 0–6 versus 7, light 12/six-tick lag, strong 30/15-tick lag, locked recovery, invalid data, and mid-aerial save/load; `tests/sim/test_m4_combat.c` covers both aerial hit routes and encoded replay; browser startup probe and red/green landing banner/ring/countdown cover both outcomes | For the easiest drill, short hop and press strong attack airborne, then fast-fall: omit the trigger for a red `STRONG AERIAL LANDING` countdown from 30, and repeat with a fresh age-0–6 trigger for a green 15-frame `STRONG L-CANCEL LANDING`; use the light aerial to confirm the ordinary 12/six route and age-7 negative boundary |
| 30 | Ladder | playable | M4.4 | A validated upward-aerial fixture composes production full hop, three light aerials, double jump, pass-through platform ascent, strong aerial, DI/SDI, and typed top-blast KO without a technique-only action | `tests/sim/test_m4_combat.c` proves three legal 4% upward aerials carry the target by more than two units and above the platform before the 12% strong-aerial finisher takes the stock at 24%, keeps the defender in hitlag/hitstun throughout, saves after hit two and compares every future hash, and proves outward DI after first contact makes an active follow-up aerial whiff before the defender becomes actionable; `src/web_client/m4_playtest.c` repeats the conversion and escape before restoring default content | In the ladder fixture, full hop and press light aerial on each earliest airborne recovery, use the double jump after hit two, then press strong aerial after hit three; confirm the target is carried above the platform and loses a stock at the upper blast line. Repeat while holding outward DI after the first hit and confirm the follow-up aerial becomes active but whiffs before the target escapes |
| 31 | Ledge-cancelling | playable | M4.4 | Production support loss cancels `SPECIAL LANDING` into ordinary `AIRBORNE`; a stationary narrow-platform fixture composes full hop, directional air dodge, waveland momentum, traction, and the support-edge transition without a technique-only action | `tests/sim/test_m4_movement.c` proves the near-edge route cancels on its first recovery tick, compares every future hash from landing through the airborne continuation, and proves the same input at platform center remains locked for all ten ticks; `src/web_client/m4_playtest.c` repeats both routes in the WebAssembly-facing sim and restores default content | In the ledge-cancel fixture, full hop through the platform, then air dodge down-right while descending so the landing begins beside its right edge; confirm one tick of retained slide carries the fighter off support and changes `SPECIAL LANDING` to `AIRBORNE`. Repeat at platform center and confirm action ticks 0–9 complete before idle |
| 32 | Mindgame | playable | M4.4 | Reduced-stick approach cue, deterministic jab-versus-shield responder policy, safe-spacing ground counter, and rear-side aerial cross-up; broader conditioning history and grab mixups remain | `tests/sim/test_m4_combat.c` independently covers the jab-read approach conversion, strong-into-shield wrong read, rear cross-up, early wrong-side whiff, front-block control, and save/load continuations; `src/web_client/m4_playtest.c` requires all constituent probes together before `mindgame_probe=1` | Show the same reduced-stick approach cue, then stop outside jab range and strong-counter when the defender jabs; when the defender instead holds shield, face away and short-hop cross-up behind. Strong into shield and an immediate wrong-side aerial are the two failed reads |
| 33 | Moonwalk | playable | M4.4 | During production initial dash, the original fighter's data-defined two-tick shallow-back setup followed by full back implements the researched [Moonwalk](https://www.ssbwiki.com/Moonwalk) sequence through explicit `MOONWALK SETUP`/`MOONWALK` actions, preserves forward facing, applies backward initial-dash speed, and exits into the ordinary traction slide; immediate full back and a one-tick shallow setup remain ordinary dashbacks | `tests/sim/test_m4_movement.c` covers default/invalid authored timing, isolated content hashing, exact setup and activation frames, facing/dash-direction/velocity, traction exit, both mistimed dashback controls, and a 694-byte mid-setup save/load with equal future hashes; `src/web_client/m4_playtest.c` repeats the positive and both negative routes before readiness; `tools/verify_m4_movement.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | Dash forward, hold Shift plus the opposite direction for two simulation ticks, then release Shift while keeping that direction held. Confirm `MOONWALK` slides backward without changing facing, release to see the traction slide, then compare immediate full-back and only one Shift-back tick; both should turn around into ordinary `INITIAL DASH` |
| 34 | Powershield | playable | M4.4 | Four-tick physical window and two-tick projectile-reflection window share the dense production shield; physical attacks take zero shield damage with ordinary powershield stun/pushback, while a fresh projectile powershield reverses ownership and velocity with no damage | `tests/sim/test_m4_combat.c` covers physical positive/negative timing; `tests/sim/test_m4_projectile.c` covers exact two-frame reflection, ordinary projectile block, returned hit, save/load/replay, and RL visibility; `src/web_client/m4_playtest.c`; `tools/verify_m4_combat.sh`; `tools/verify_m4_projectile.sh`; `tools/verify_m4_browser.sh` | Raise shield immediately before a physical attack connects; confirm powershield indicator and zero shield damage, then raise it as the Pulse Bolt arrives and confirm the bolt changes owner and reverses direction without damage. Raise shield too early for the ordinary block controls |
| 35 | Pivoting | playable | M4.4 | The production initial-dash reversal supplies the one-tick pivot window; neutral or a ground action on the next tick preserves reversed facing and residual momentum, while held reversal continues dash and post-window reversal enters run turnaround | `tests/sim/test_m4_movement.c` covers exact pivot timing, empty pivot, immediate attack, facing/momentum preservation, held and post-run negative routes, and mid-pivot save/load hash continuation; `src/web_client/m4_playtest.c` repeats those routes before browser readiness | Dash, tap the opposite full direction for exactly one tick, then return to neutral and immediately attack; confirm the new facing and backward slide persist. Repeat without attacking for an empty pivot, hold the reversal to continue dashing, and try after `RUN` to confirm `RUN TURNAROUND` instead |
| 36 | Planking | playable | M4.4 | A dedicated data-defined 29-tick disabled-regrab period, 37-tick ledge invulnerability, ordinary ledge drop/double jump/regrab, and responding-opponent hit resolution compose into a bounded refresh loop; a future tournament ruleset may add a separate ledge-grab limit | `tests/sim/test_m4_movement.c` performs three exact-boundary refresh cycles, rejects the geometrically legal tick-28 catch while one lockout tick remains, saves on release and compares every future hash through all regrabs, rejects an active jab on each refreshed catch, and proves a two-tick fast-fall mistiming is hit after invulnerability expires; `src/web_client/m4_playtest.c` repeats both routes before restoring default content and browser readiness | In the planking fixture, grab the right ledge, drop, immediately double jump, and regrab on the first legal frame while the opponent jabs; repeat three times and confirm each catch refreshes the invulnerability ring. Fast-fall for the final two frames on the negative route and confirm the missed ledge refresh is punishable |
| 37 | Power shield canceling | playable | M4.4 | Physical powershield result, data-driven one-frame release delay, and the shared standing-action selector for neutral/up/down light plus direct strong attacks; the tactic adds no technique-only state | `tests/sim/test_m4_combat.c` covers positive/negative timing, each current cancel-action selection, and focused replay through independently tested constituent mechanics; browser shield/PSC startup probe enters the directional router | Powershield the physical attack, release shield before shield stun ends, leave frame 1 of `SHIELD RELEASE` neutral, then press neutral light, full-up light, full-down light, or direct strong on frame 2; repeat after an ordinary block and confirm the full 15-tick release cannot be canceled |
| 38 | Scar Jump | playable | M4.4 | The original fighter's data-authored normal wall jump composes with the production right-ledge jump and raised-block wall: a fresh full-away stick edge at contact enters a 24-tick `WALL JUMP`, applies four ticks of intangibility, and preserves the midair jump for an immediate jump or aerial cancel, matching the researched [Scar Jump](https://www.ssbwiki.com/Scar_Jump) without a technique-only shortcut | `tests/sim/test_m4_movement.c` reaches the wall from ordinary ledge input, proves exact velocity/facing, saved-air-jump preservation, authored lock/intangibility, jump and aerial cancels, a 694-byte mid-action save/load with equal future hashes, invalid data, and the early-away missed-contact control; `src/web_client/m4_playtest.c` independently repeats the aerial-cancel route and missed-contact control before browser readiness; `tools/verify_m4_movement.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | Grab the right ledge, wait through ledge catch, press Jump, then hold inward/left until the fighter contacts the raised block's right wall. Freshly press full right to enter `WALL JUMP`; immediately press Jump to spend the saved air jump or Attack to cancel into an aerial. Hold right immediately after leaving the ledge for the missed-contact comparison |
| 39 | Sharking | playable | M4.4 | One-way platform geometry, ordinary full-hop aerial initiation from below, moving-platform air steering, and target shield pressure; broader upward attacks and opponent movement mixups remain | `tests/sim/test_m4_combat.c` covers below-platform aerial initiation, hit and too-early whiff routes, held-shield pressure, and mid-aerial save/load with 32 future hashes; `src/web_client/m4_playtest.c` repeats all three outcomes on the default moving platform before browser readiness | Put the target on the pass-through platform, drop the attacker to the floor, move underneath, then full hop and aerial upward through it; confirm the target is hit. Attack immediately after takeoff to whiff below the platform, then repeat against held shield and confirm shield damage with zero percent |
| 40 | Shield break combo | playable | M4.4 | Production shield depletion and hit-caused break route through upward launch, gravity, landing, down/stand phases, percent-scaled vulnerable stun, fresh-input mash reduction, interruption, and 30-HP reset | `tests/sim/test_m4_combat.c` covers phase order, early invulnerability, vulnerable punish, exact fresh/held mash behavior, reset, and mid-stun save/load continuation; `src/web_client/m4_playtest.c` and `tools/verify_m4_browser.sh` cover the ordinary-input depletion route and browser recovery visualization | Hold shield to force a break or attack a nearly empty shield, follow `SHIELD BREAK` through `DOWN` and `STAND`, then punish during `SHIELD BREAK STUN`; as defender, alternate fresh buttons or stick flicks and watch the `MASH · Nf` counter fall faster, while held input receives no repeat reduction |
| 41 | Shield platform dropping | playable | M4.4 | A validated reduced-down analog band drops an already-shielding fighter through pass-through support using the production nine-tick platform-pass route; full down remains spot dodge, shield release remains grounded, and no technique-only state exists | `tests/sim/test_m4_movement.c` covers same-tick shield-entry rejection, both exact band edges, below-band shield, full-down spot dodge, solid-floor exclusion, ordinary shield release, invalid data, and mid-route save/load with 24 future hashes; `src/web_client/m4_playtest.c` repeats the ordinary reduced-down, below-band, and full-down routes before browser readiness | Land on the moving platform, hold shield, then use reduced down (`Shift+S` / `Shift+Down`, or a matching analog tilt) and confirm immediate `AIRBORNE` fall-through. Repeat with full down for `SPOT DODGE`, with too little tilt to remain `SHIELD`, and by releasing the trigger for ordinary `SHIELD RELEASE` |
| 42 | Shield-stop | playable | M4.4 | Run, dense shield, retained momentum, traction; initial dash exclusion | `tests/sim/test_m4_combat.c` operations `shield-stop-and-entry` and `initial-dash-cannot-shield` | Reach `RUN`, press shield, and confirm forward shield slide under traction; press it during `INITIAL DASH` for the negative case |
| 43 | Shine spike | playable | M4.4 | The original data-defined Prism Burst has distinct grounded/aerial actions, a two-tick physical hitbox with downward launch, and active-box projectile reflection; ordinary down-plus-special input composes with the production air route, victim recovery policy, blast zones, stocks, and typed KO events | `tests/sim/test_m4_reflector.c` proves grounded hit data, reflector-owned projectile reversal and returned hit, an ordinary-input offstage downward hit into KO, the unchallenged recovery control, 694-byte save/load future equality, replay verification, and structured/compact RL visibility; `src/web_client/m4_playtest.c` repeats the positive and recovery-control routes before readiness; `tests/web/test_m4_playtest.c` exercises the live down-special action; `tools/verify_m4_reflector.sh` and `tools/verify_m4_browser.sh` compile strict native oracles | Knock the opponent offstage, follow them, then hold down and freshly press special (`E`, `;`, or top face) as their recovery rises into range. Confirm `PRISM BURST AIR`, the downward typed hit, and the stock loss; reset and leave the same victim unchallenged to confirm they recover. Fire a Pulse Bolt at an active Prism Burst to confirm ownership and horizontal velocity reverse without a powershield |
| 44 | Short hop laser | playable | M4.4 | The binary short hop composes with the original fixed-capacity Pulse Bolt, fresh special input, distinct grounded/aerial fire actions, authored eight-tick recovery, straight projectile motion, and generic landing; only one projectile may own the canonical slot | `tests/sim/test_m4_projectile.c` proves deterministic slot arbitration, grounded and short-hop fire, typed fire/hit/reflect events, ordinary shield block, exact powershield reflection, returned hit, landing, 694-byte save/load future equality, replay verification, and structured/compact RL visibility; `src/web_client/m4_playtest.c` requires a short-hop-fire-and-land probe before readiness; `tests/web/test_m4_playtest.c` exercises the live special ABI; `tools/verify_m4_projectile.sh` and `tools/verify_m4_browser.sh` compile strict native oracles | Tap jump and release during jump squat, then press `E` (`;` for Player 2, or top face on a Standard Gamepad) after takeoff. Confirm `PULSE BOLT AIR`, the cyan projectile and typed fire event, then generic `LANDING`; repeat from the ground for `PULSE BOLT GROUND`, and fire while another bolt is active to confirm the single-slot route does not duplicate it |
| 45 | Short hop air dodge | playable | M4.4 | Binary short hop, fresh-trigger directional/neutral air dodge, fixed-vector normalization and decay, exact invulnerability, helpless transition, and special landing are production mechanics | `tests/sim/test_m4_movement.c` covers the ordinary-input first-airborne-frame route, neutral/directional cases, held-trigger rejection, facing, invulnerability, save/load, and landing; `tests/sim/test_m4_combat.c` covers hit rejection/acceptance; browser air-dodge startup probe and 180-tick replay corpus | Tap jump, release during the three-tick jump squat, then press a direction plus a fresh trigger on the first airborne frame; confirm `AIR DODGE` (or immediate `SPECIAL LANDING` when aimed down), and repeat with the trigger already held to confirm no new dodge |
| 46 | Short hop fast fall l-cancel | playable | M4.4 | Binary short hop, light or strong aerial attack, fast fall, seven-frame L-cancel input, and reduced landing state compose through ordinary input | `tests/sim/test_m4_movement.c` covers the complete light SHFFL route plus strong 30/15-tick practice route and no-trigger comparisons; `tests/sim/test_m4_combat.c` records and verifies the light route with per-tick hashes; browser startup probe performs both strong landing paths | Tap jump and release during jump squat, press the strong aerial, hold down after the apex to fast-fall, then tap the trigger within seven ticks of landing; confirm the green 15-frame result, and omit the trigger to confirm the red 30-frame result; repeat with light attack for the ordinary six/12-frame route |
| 47 | Small step forward smash | playable | M4.4 | A data-defined three-tick forward-smash input window converts full-direction plus light attack into the production strong attack from standing or initial dash; initial-dash momentum and traction extend the same authored hitbox without presentation shortcuts | `tests/sim/test_m4_combat.c` proves the standing forward smash misses at the range where the three-frame delayed version hits, covers exact frame-3/frame-4, missing-direction, and one-frame opposite-direction boundaries, validates content data, and checks mid-route save/load future hashes; `src/web_client/m4_playtest.c` repeats standing, frame-3, frame-4, and neutral-direction routes before browser readiness | Hold a full direction and press light simultaneously for the standing forward smash. Reset, tap/hold the direction, wait one to three simulation ticks, then press light and confirm the fighter enters `STRONG ATTACK` farther forward. Wait four ticks or release the direction before light attack to confirm the ordinary non-smash attack instead |
| 48 | Smash directional influence | playable | M4.4 | Target hitlag with two-axis component-edge SDI/ASDI; grounded shield hitlag with horizontal-only 0.66-scaled shield SDI/ASDI and ignored vertical input; collision-safe displacement | `tests/sim/test_m4_combat.c`; browser reaction and shield startup probes; 180-tick replay corpus | During ordinary hitlag, cross one component, hold it, then add the second; while blocking, cross horizontal once, prove hold/vertical do not repeat, and retain horizontal input for shield ASDI |
| 49 | Spacing | playable | M4.4 | The production jab reaches 1.8 center-to-center units and the strong attack reaches 2.1; ordinary standing attacks, recovery, shield block, and a deterministic jab-first responder compose into a legal distance-dependent exchange | `tests/sim/test_m4_combat.c` proves a 1.95-unit jab whiff into strong counter during recovery, the 1.7-unit close jab punish, the 2.25-unit double whiff, a safe-tip shield block, and mid-counter save/load with 32 future hashes; `src/web_client/m4_playtest.c` reaches all three bands by ordinary reduced-stick walking and repeats the exchanges before browser readiness | Stand just outside the opponent's light-attack hitbox but inside your strong-attack reach. Have the opponent jab, then counter with strong attack while their whiff recovers; move closer and confirm the jab hits first, move farther and confirm the strong counter also misses, then shield at the safe distance to confirm the same strong tip still contacts |
| 50 | Stage humping | playable | M4.4 | The original fighter's authored 0.1-unit one-tick crouch step implements the repeated-input basis of researched [stage humping](https://www.ssbwiki.com/Stage_humping): each fresh diagonal-down edge from standing or crouch enters explicit `CROUCH STEP`, advances once, and settles into ordinary `CROUCH`; a held diagonal cannot repeat without release | `tests/sim/test_m4_movement.c` proves default/invalid data, isolated content hashing, exact positive and negative displacement, eight release/reset repetitions, held-diagonal non-repetition, neutral-down and horizontal-only controls, and a 694-byte mid-step save/load with equal future hashes; `src/web_client/m4_playtest.c` independently repeats the positive and all three negative routes before browser readiness; `tools/verify_m4_movement.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | From standing, tap diagonal down-forward or down-back, release, and repeat to produce one short crouched step per fresh input. Hold the diagonal to confirm only one step occurs, press neutral down to confirm ordinary stationary crouch, and press horizontal alone to confirm ordinary dash |
| 51 | Stage spike | playable | M4.4 | The researched [stage spike](https://www.ssbwiki.com/Stage_spike) composes the original Vector Ascent recovering target with production aerial launch, the raised block's solid wall/ceiling, missed-impact bounce, successful surface tech, blast zones, and stock/KO resolution. The recovery resource remains spent through the impact, so an unteched outward bounce can deny the return without a stage-spike-only branch | `tests/sim/test_m4_movement.c` independently proves the recovering action and spent-resource persistence; `tests/sim/test_m4_combat.c` proves upward/sideways launch into the raised block plus exact `WALL_BOUNCE`/`CEILING_BOUNCE` and fresh-trigger `WALL_TECH`/`CEILING_TECH` controls; `tests/sim/test_m4_match.c` covers the resulting blast-zone stock path; browser readiness exposes the recovery and surface-impact primitives separately. Per the emergent-technique policy, these constituent oracles are reused instead of adding a stage-spike-specific harness | Build enough damage, knock the opponent past the right edge, and have them steer `VECTOR ASCENT` inward beside the raised block's outside wall. Follow with an aerial that sends them into the wall; omit the trigger and confirm `WALL BOUNCE` redirects them outward with recovery still spent, leading to a missed return or stock loss. Reset and freshly press the trigger before impact to confirm `WALL TECH` prevents the bounce; the same comparison works beneath the block with `CEILING BOUNCE` versus `CEILING TECH` |
| 52 | Stalling | playable | M4.4 | The researched [stalling](https://www.ssbwiki.com/Stalling) tactic emerges from the already-playable ledgestall/planking route: ordinary ledge drop, double-jump consumption, the 29-tick regrab lockout, refreshed 37-tick protection, and a responding opponent; no stalling-only state or input is required | Existing `tests/sim/test_m4_movement.c` Planking evidence performs three exact-boundary time-extending cycles while Player 2 attacks, proves each resource/protection refresh, and shows the same responder punishing a two-tick mistiming; the existing browser `planking_probe` repeats both outcomes before readiness | Grab the ledge, repeatedly drop, immediately double jump, and regrab on the first legal tick while the opponent attacks. Confirm the loop delays engagement while consuming and refreshing the air jump/protection; then fast-fall during the final two lockout ticks and confirm the responder can punish the missed refresh |
| 53 | Taunt cancelling | playable | M4.4 | Input schema 5 supplies a dedicated fresh taunt edge; the original fighter authors a 90-tick grounded `TAUNT` that preserves and decelerates inherited dash momentum, locks ordinary action routers, and yields to the existing support-edge `TEETER` transition | `tests/sim/test_m4_movement.c` proves default/invalid duration, isolated content hashing, dash-momentum entry, exact full duration, attack/jump lock, held-button non-repetition, a 694-byte mid-taunt save/load with equal future hashes, and edge cancellation; `src/web_client/m4_playtest.c` repeats full-duration and edge-cancel routes before browser readiness; `tools/verify_m4_movement.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | Dash toward the floor edge, release horizontal input and press Taunt just before retained momentum crosses the support bound; confirm the edge transition replaces `TAUNT` with `TEETER` well before 90 ticks. Reset and press Taunt away from the edge to confirm all 90 ticks complete, movement/attack inputs do not interrupt it, and holding Taunt does not retrigger without release |
| 54 | Team wobble | playable | M4.4 | The production four-player team mode, same-team grab rejection, reciprocal capture links, low down throw, hitlag, grab recovery, and fresh trigger-plus-light grab compose into an alternating handoff without a technique-only action; the narrow static-stage lab assigns allied P1/P3 to the two physical controllers, scripts only P2's legal mash edges, and leaves P4 neutral | `tests/sim/test_m4_combat.c` performs two alternating low-throw/fresh-grab handoffs, verifies both typed throws and reciprocal captures while the victim mashes, and proves an early waiting grab spends its window and misses; browser startup independently repeats both routes, and `tests/web/test_m4_playtest.c` checks the four-player view plus Player-2-controller-to-P3 mapping | Click `Team Wobble Lab`. P1 and P3 are allies: after either captures P2, hold down and freshly press Attack with the holder while the other ally freshly presses light plus shield; alternate sides after each handoff. Start the waiting ally's grab early for the negative route and confirm P2 escapes instead |
| 55 | Teching | playable | M4.4 | Ground tech in place/roll, wall tech, wall-tech jump, ceiling tech, missed floor/wall/ceiling outcomes, lockout, and exact invulnerability are production-path mechanics; complete replay/rollback and owner evidence remains | `tests/sim/test_m4_combat.c` covers positive/negative ground, wall, ceiling, and bounce outcomes; browser reaction/floor/surface probes; native deterministic trace | Trigger within 20 ticks of a tumble landing for in-place/roll, or strong-launch a tumbling target into the raised block and use a fresh trigger for wall/ceiling tech; hold up for wall-tech jump and repeat without the input for the corresponding missed-tech outcome |
| 56 | Tech-chasing | playable | M4.4 | Production tumble landing, neutral and directional tech choices, exact recovery invulnerability, ordinary chaser movement, and the standing jab compose into a reaction-driven punish route | `tests/sim/test_m4_combat.c` follows airborne movement into observed tech-in-place and right-roll outcomes, attacks only after the 20-tick invulnerability boundary, proves a same-time non-following jab misses the roll, and checks mid-roll save/load future hashes; `src/web_client/m4_playtest.c` repeats all three ordinary-input routes before browser readiness | Strong-launch the opponent, follow their airborne path, observe neutral `TECH IN PLACE` or directional `TECH ROLL`, then continue moving and jab after the gold invulnerability ring clears but before recovery ends. Repeat the roll without following and jab at the same action tick to confirm the whiff |
| 57 | Teeter cancel | playable | M4.4 | The original fighter's authored snap distance and duration implement the researched [teeter cancel](https://www.ssbwiki.com/Teeter_cancel): releasing horizontal input while grounded momentum crosses a support edge clamps the fighter into explicit `TEETER`, while continued outward input still runs off; all ordinary standing routers remain available immediately | `tests/sim/test_m4_movement.c` proves default/invalid data, isolated content hashing, exact support-edge clamp, grounded/facing/velocity state, 30-tick neutral duration, standing-attack and reverse-dash cancels, held-outward and early-release negatives, and a 694-byte mid-teeter save/load with equal future hashes; `src/web_client/m4_playtest.c` independently repeats both cancels and both negatives before browser readiness; `tools/verify_m4_movement.sh` and `tools/verify_m4_browser.sh` compile the strict native oracles | Dash toward the floor edge, release horizontal input just before residual momentum would carry the fighter off, and confirm `TEETER` clamps at the edge. Immediately press Attack for a standing attack or full opposite direction for a reverse dash with no run-brake wait. Hold outward instead to confirm the ordinary run-off, and release too early to confirm the fighter stops short |
| 58 | Turtling | playable | M4.4 | The researched [turtling](https://www.ssbwiki.com/Turtling) tactic is avoiding the opponent, using ranged attacks, and punishing bad approaches. The existing one-slot Pulse Bolt, grounded fire recovery, movement/spacing, fixed match clock, and continuous approach-and-jab responder compose that strategy without a turtling-only action | Existing `tests/sim/test_m4_projectile.c` Camping evidence runs 180 production ticks: seven legal fires produce six hits while the defender stays outside melee range and takes zero damage; omitting projectiles lets the identical responder close and land three jabs. Existing Approach/Spacing evidence independently proves the safe whiff punish, and browser `camping_probe` repeats both bounded policies | Keep a small lead and move to one side. Fire a fresh Pulse Bolt only after the prior one resolves while preserving space from an approaching opponent; punish unsafe close approaches. Reset, omit every projectile against the same approach, and confirm the opponent reaches and hits you |
| 59 | V-cancelling | playable | M4.4 | The existing full-trigger age and 40-tick tech lockout now gate a three-tick eligible-air-state check; a qualifying hit scales both pending launch components to 95% without changing hitstun, tumble, or canonical state | `tests/sim/test_m4_combat.c` covers positive `AIRBORNE`, `FALL_SPECIAL`, and vulnerable `AIR_DODGE` routes, ages 0–2, the exact age-3 boundary, grounded and aerial-attack exclusions, repeated-trigger lockout, exact two-axis scaling, unchanged hitstun/tumble, invalid data, byte-identical events, and mid-route save/load future hashes; `src/web_client/m4_playtest.c` compares ordinary, V-cancelled, attacking, and lockout routes through default ordinary input before browser readiness | Move both fighters close and airborne. Start the attack, then fully press the defender's trigger on the collision frame or either of the prior two frames; compare the resulting launch vector with the no-trigger route. Repeat while the defender is attacking or after another full trigger inside the 40-tick lockout and confirm the launch is not reduced |
| 60 | Wavedash | playable | M4.4 | Binary short hop plus normalized directional air-dodge momentum converts on floor contact into a ten-tick locked special landing that preserves horizontal velocity and slides under fighter traction | `tests/sim/test_m4_movement.c` covers ordinary-input diagonal wavedash, facing lock, exact landing lag, momentum/traction slide, neutral negative case, and save/load; browser adapter directly performs the route and its startup probe also reaches `SPECIAL LANDING`; replay corpus observes air dodge and special landing | Tap and release jump during jump squat; on the first airborne frame hold down-left or down-right and press a fresh trigger. Confirm `SPECIAL LANDING`, horizontal slide with unchanged facing, action ticks 0–9, then idle; compare a neutral air dodge, which does not create the diagonal slide |
| 61 | Zero-to-death combo | playable | M4.4 | The validated fast-jab fixture, production hitlag/hitstun, DI/SDI, strong finisher, stocks, and typed KO result compose from an exact 0% start without a technique-only action | `tests/sim/test_m4_combat.c` proves 21 legal jabs plus the strong finisher remain one uninterrupted 0%-to-138% sequence, saves after hit 11 and compares every future hash through the attacker-attributed KO, and proves outward DI after first contact breaks the chain before the finisher while a later active jab whiffs; `src/web_client/m4_playtest.c` repeats the conversion and DI escape in the WebAssembly-facing sim and restores default content before browser readiness | In the zero-to-death fixture, begin at 0% and press jab again at each earliest recovery, then use strong after jab 21; confirm the target remains in hitlag/hitstun throughout and loses a stock at 138%. Repeat while holding outward DI after the first hit and confirm the chain breaks, a later active jab whiffs, and the target survives |

## Current gate summary

The registry currently has 1 `verified`, 60 `playable`, 0
`primitive-ready`, and 0 `planned` rows. M4 acceptance is therefore blocked.
Advancing a row requires adding its exact evidence here in the same change;
adding a primitive without updating this registry is a plan-compliance failure.
